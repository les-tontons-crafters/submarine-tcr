build:
  - os: [darwin, linux, windows]
    arch: ["386", amd64, arm64]
    command: pytest
    arguments: [--collect-only]
test:
  - os: [darwin, linux, windows]
    arch: ["386", amd64, arm64]
    command: pytest
    arguments: []
  - os: [ darwin, linux, windows ]
    arch: [ "386", amd64, arm64 ]
    command: behave
    arguments: [ ]
test-result-dir: pytest
